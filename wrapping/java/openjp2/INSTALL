In order to deploy JNI components on GAEL repository you
need to specify a property:
 - the directory location of openjpeg libraries 'openjpeg.library.dir'.
And to set an available maven configuration for a server access nammed
'gael-main'

example settings.xml:
...
<servers>
   ...
   <server>
      <id>gael-main</id>
      <username>user</username>
      <password>****</password>
   </server>
</servers>
...

example command line:
  mvn clean deploy -Dopenjpeg.library.dir=<path/to/directory>

