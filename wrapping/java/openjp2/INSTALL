In order to deploy JNI components on GAEL repository you
need to specify the directory location of openjpeg
libraries via the property 'openjpeg.library.dir' and
use the command line:
  mvn deploy -Dopenjpeg.library.dir="path/to/directory"

