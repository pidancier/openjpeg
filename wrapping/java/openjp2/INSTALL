In order to deploy JNI components on GAEL repository you
need to specify two properties:
 - the directory location of openjpeg libraries 'openjpeg.library.dir'.
 - the server access id to GAEL repository 'server.id' (see maven settings.xml).

example:
  mvn clean deploy -Dopenjpeg.library.dir=<path/to/directory> -Dserver.id=<serverId>

